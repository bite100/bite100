# 网络选择：sepolia | mainnet | polygon
VITE_NETWORK=sepolia

# P2P Relay 配置（路径 2：浏览器连公共 relay，多地址依次 fallback）
VITE_P2P_WS_URL=ws://localhost:8080/ws
# 生产示例：VITE_P2P_WS_URLS=wss://relay1.p2p-p2p.xyz/ws,wss://relay2.p2p-p2p.xyz/ws
VITE_P2P_WS_URLS=
VITE_P2P_API_URL=http://localhost:8080

# 节点 API（多节点支持，逗号分隔）
VITE_NODE_API_URL=http://localhost:8080,http://localhost:8081

# WalletConnect 项目 ID（可选；配置后前端会显示「WalletConnect / 钱包 App」按钮）
# 在 https://cloud.walletconnect.com 创建项目后，将 Project ID 填到这里
VITE_WC_PROJECT_ID=
