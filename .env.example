# P2P 生产/开发环境变量示例
# 复制为 .env 后修改：cp .env.example .env
# 使用：docker compose -f docker-compose.prod.yml --env-file .env up -d

# 网络：sepolia | mainnet | polygon
NETWORK=sepolia

# RPC（按网络选择）
SEPOLIA_RPC_URL=https://ethereum-sepolia.publicnode.com
# MAINNET_RPC_URL=https://eth.llamarpc.com
# POLYGON_RPC_URL=https://polygon-rpc.com

# 领奖钱包地址（必填）。可通过环境变量 REWARD_WALLET、配置文件 reward_wallet 或启动参数 -reward-wallet 设置；未设置则节点拒绝启动。
# 可选：自动提交证明领奖时再配置 REWARD_ETH_PRIVATE_KEY（该地址对应私钥，保密）
REWARD_WALLET=0xYourRewardAddressHere

# Bootstrap 节点（多节点时填，格式：/ip4/xx.xx.xx.xx/tcp/4001/p2p/Qm... 多个用逗号或空格）
# BOOTSTRAP_NODES=/ip4/1.2.3.4/tcp/4001/p2p/QmXXX

# 日志级别：debug | info | warn | error
LOG_LEVEL=info
