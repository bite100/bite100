# ============================================================
# P2P 生产/开发环境变量
# 使用：复制为 .env，改 REWARD_WALLET 后执行
#       docker compose -f docker-compose.prod.yml --env-file .env up -d
# ============================================================

# 网络：sepolia | mainnet | polygon
NETWORK=sepolia

# RPC（一般用默认即可）
SEPOLIA_RPC_URL=https://ethereum-sepolia.publicnode.com

# 【必填】领奖钱包地址（0x 开头），用于领取节点奖励
REWARD_WALLET=0xYourRewardAddressHere

# 以下为可选
# REWARD_ETH_PRIVATE_KEY=   # 自动提交证明领奖时填（该地址私钥）
# BOOTSTRAP_NODES=          # 多节点时填其他节点地址
# LOG_LEVEL=info
