# 跨链桥接快速开始

> 版本：v1.0  
> 更新日期：2025-02-08

---

## 一、快速开始（5 分钟）

### 1. 安装依赖

```powershell
cd contracts
.\scripts\install-layerzero.ps1
```

### 2. 部署到测试网

```powershell
# Sepolia
.\scripts\deploy-crosschain-bridge.ps1 -Chain sepolia

# Base Sepolia
.\scripts\deploy-crosschain-bridge.ps1 -Chain base_sepolia
```

### 3. 配置代币映射

```powershell
.\scripts\configure-bridge-tokens.ps1 `
    -BridgeAddress 0x你的桥接合约地址 `
    -Chain sepolia `
    -Token 0x源链代币地址 `
    -DstChainId 84532 `
    -DstToken 0x目标链代币地址
```

### 4. 测试

打开前端应用，切换到"跨链桥"标签页，发起跨链转移。

---

## 二、详细步骤

详见 [跨链桥接部署测试指南](./跨链桥接部署测试指南.md)

---

*快速开始指南，帮助你在 5 分钟内完成跨链桥接的部署和测试。*
