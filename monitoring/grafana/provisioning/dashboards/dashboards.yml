# Grafana 仪表板自动加载
apiVersion: 1
providers:
  - name: 'P2P'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
